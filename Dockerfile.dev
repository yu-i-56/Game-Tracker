# ========================================
# 開発環境用のDockerfile
# ========================================
FROM node:20-alpine

# 作業ディレクトリを設定
WORKDIR /app

# package.jsonとpackage-lock.jsonをコピー
COPY package.json package-lock.json ./

# 依存関係をインストール
# 開発環境なので devDependencies も含む
RUN npm ci

# Prismaクライアントを生成
# 初回のみ必要だが、ここで実行しておく
COPY prisma ./prisma
RUN npx prisma generate

# アプリケーションがリッスンするポートを公開
EXPOSE 3000

# 開発サーバーを起動
# ホットリロードが機能するように、ソースコードはボリュームマウントする
CMD ["npm", "run", "dev"]

